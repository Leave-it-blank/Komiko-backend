import { JSX } from "preact";
import { Upload } from "upload-js";
import { UploaderOptionsRequired } from "../../../UploaderOptions";
import "./UploaderWidget.scss";
import { UploaderResult } from "../../../components/modal/UploaderResult";
interface Props {
    options: UploaderOptionsRequired;
    reject: (error: Error) => void;
    resolve: (files: UploaderResult[]) => void;
    upload: Upload;
}
export declare const UploaderWidget: ({ resolve, options, upload }: Props) => JSX.Element;
export {};
