import { Upload, UploadConfig } from "upload-js";
import { UploaderOptions } from "./UploaderOptions";
import { UploaderResult } from "./components/modal/UploaderResult";
export declare class Uploader {
    private readonly upload;
    private readonly inlineWidgets;
    constructor(uploadOrConfig: UploadConfig | Upload);
    open(optionsMaybe?: UploaderOptions): Promise<UploaderResult[]>;
    /**
     * Required when the 'uploader.open()' method is called from within '<head>'.
     */
    private getBody;
}
