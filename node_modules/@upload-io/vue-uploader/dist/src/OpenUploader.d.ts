import { Uploader, UploaderOptions, UploaderResult } from "uploader";
import { PreventableEvent } from "../PreventableEvent";
export declare function openUploader({ event, uploader, options, onComplete }: {
    event?: PreventableEvent;
    onComplete?: (files: UploaderResult[]) => void;
    options?: UploaderOptions;
    uploader: Uploader;
}): void;
